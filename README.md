# Access-the-elements-of-an-array-with-user-input.
This shows ways to get array elements with user input.
The Python and C++ files do not use loops works best for small arrays.
The C file uses 2 arrays and a for loop and is best for small arrays.
The C and C++ files must be compiled before use.
